void calculation(star* stars, star* stars_buffer, int N_stars, double delta_t);

void keep_within_box(float* xA, float* yA);

int galsim(int N, char * filename, int steps, double delta_t, int graphics);